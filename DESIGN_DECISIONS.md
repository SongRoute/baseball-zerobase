# DESIGN_DECISIONS

> 확정된 설계 결정 + 그 이유 + **금지사항**의 단일 출처. 에이전트의 헌법.
> 에이전트는 모든 작업 시작 시 이 문서를 읽고, "금지사항"을 절대 위반하지 않는다.
> 결정 변경이 필요하면 임의로 바꾸지 말고 사람(Song)에게 먼저 확인한다.
> 결정·평가는 Song이 한다. 구현·실행·실험은 에이전트가 한다.
>
> 형식: 각 결정은 [확정 / 잠정 / 미해결] 상태를 가진다. 잠정·미해결은 실험·데이터 확인 후 확정.

---

## D1. 제품 포지션 [확정]

- 추천의 "정확성/최적성"을 주장하지 않는다. "데이터상으론 이것이 정배"라고 제시하고 판단은 시청자에게 맡긴다.
- 평가 포지션: **MLB 집단지성과의 정합성 + 야구 상식과의 부합**. (baseline 논문도 동일한 한계에서 이 방식을 택함)
- 이유: 반사실(다른 공을 던졌다면?)은 검증 불가능. 최적성 증명은 baseline 논문도 future work로 미뤘다.

## D2. 데이터 [확정]

- 소스: MLB Statcast (pybaseball), 투구 단위(pitch-level).
- 라이브 서빙: MLB StatsAPI (Gameday), 폴링, 투구 종료 후 갱신(5~15초 지연). 추론에서만 사용.
- 학습 범위: 2022~2026(최대 5년). 검증은 과거 완결 시즌(우선 2024) 1개. 최종 모델은 2026 정규시즌으로 9월 재생성.

## D3. 학습/서빙 일관성 [확정] — 최우선 원칙

- **학습 입력 = 라이브 입력.** 라이브(StatsAPI)에서 안정적으로 못 받는 피처는 학습에서도 제외한다.
- 두 경로는 동일한 "공유 상태공간 스펙"과 동일한 룩업 테이블을 공유한다.
- 어댑터 레이어가 StatsAPI live JSON → 상태벡터 변환을 담당하며 모든 스큐를 흡수한다.

## D4. 상태공간 [잠정 — 점수차 구간 등 세부 미확정]

추론 시 입력:
- base-out (주자 × 아웃)
- 볼카운트 / 스트라이크
- 이닝
- 점수차 (raw 아닌 구간화 — 구간 기준 미확정)
- 투수 클러스터 ID
- 타자 약점유형 클러스터 ID + 타자 위협도(연속값)
- 개인 보조 피처: 해당 선수의 시즌 구사율/성적 벡터(클러스터 위에 개인차)

## D5. 행동공간 [확정]

- 6구종 × 13존 = 78 행동.
- 13존 = 9존(스트라이크존 3×3) + 외곽 상하좌우 4존. 볼/유인구 표현 위해 외곽 필수.
- **존은 타자 기준 상대좌표**(핸드니스 상대). 좌타 "몸쪽"과 우타 "몸쪽"은 절대좌표상 반대지만 둘 다 "몸쪽"으로 표현·처리.
- "승부 회피/거르기"가 외곽 존 가치로 충분히 표현되는지는 데이터로 확인 [미해결].

## D6. 모델 접근: 풀 RL [확정]

- 단순 분류기(즉시결과 예측) **기각**. 이유: 병살 유도(맞는 게 정답), 유인구(볼이 정답), 강타자 승부 회피를 표현 못 함.
- 타석 너머 시간지평이 필요(예: 강타자 무리한 승부 회피) → 풀 RL.

## D7. 모델 방법: 함수근사 [확정 — 구체 알고리즘은 실험]

- tabular Value Iteration **기각**. 이유: 상태(약 58만) × 행동(78) ≈ 4,500만 셀, 5년 데이터(약 350만 투구)로 못 채움.
- 함수근사로 일반화. 오프라인 고정 데이터셋이므로 FQI(Fitted Q-Iteration)가 1순위 후보.
- 구체 알고리즘(FQI 변형 등)은 `modules/q_function/EXPERIMENTS.md`에서 실험 후 선택.

## D8. 학습 산출물 두 가지 [확정]

1. **transition NN** — P(결과 | 상태, 행동). 추론 시 "실제 던질 법한 행동"을 거르는 **행동 필터**로 사용.
   - baseline(SmartPitch)의 4-클래스(strike/ball/foul/in-play) 구조를 재현·확장. 검증된 레시피 있음.
2. **Q함수** — (상태, 행동) → 기대실점. 필터된 후보를 정렬.

## D9. reward [잠정 — 귀속 방식 미해결]

- RE24(Run Expectancy) 변화량 기반. WPA 아님(점수차·이닝 의존이 커서 reward로 불안정 → WPA는 "중요도 표시"용).
- 인플레이 보상: xwOBA 사용(약한 타구 유도를 선호하게). baseline도 동일.
- **미해결**: 타석 종료 RE24 변화를 그 타석의 투구들에 어떻게 귀속할지(균등? 마지막 공 가중? 부트스트랩?).

## D10. 신뢰도 관리 [확정]

- **표본수 게이트**: 특정 (상태, 행동) 쌍 관측이 임계값 미만이면 추천 제외 또는 "데이터 부족" 표시.
- transition NN 필터가 분포 시프트(오프라인 RL 근본 문제) 1차 방어선. 필터 임계값 튜닝은 민감 → 9월 리허설 전 검증 방법 확정 [미해결].

## D11. 클러스터링 [확정 — 피처 세부는 실험]

- 투수 클러스터와 타자 클러스터는 **각각 독립적으로** 묶는다. 한 축을 다른 축에 종속시키지 않는다.
  (투수↔타자 상호작용은 클러스터링이 아니라 Q함수가 학습한다.)
- **타자 = 약점유형 클러스터 + 위협도 연속값**, 두 축 분리:
  - 약점유형(클러스터링): 구종군별(3군) 헛스윙·상대성적, 거친 존별 약점, chase rate. 순수 스타일.
  - 위협도(연속값, 클러스터링 안 함): xwOBA 등 절대 생산성. "승부 회피" 판단 근거.
- 투수: 구종 구사율 벡터(중심), 평균 구속대, 좌/우. (릴리스 특성 선택)
- 방법: UMAP + HDBSCAN(K 자동) 1순위 후보. 노이즈(미할당) fallback 규칙 필요. 구체 방법은 실험.
- 표준화 필수. 최소 타석/투구 수 필터.

## D12. 출력 [확정]

- 내부: `{pitch, zone, score}`. 표시: "직구 · 몸쪽 높게"(존→자연어, 핸드니스 반영).
- 우선순위: **Top-1 필수** → (확장) 2순위 → (확장) 가장 위험한 후보 → (후순위) LLM 자연어 해설.
- 출력 구조는 "모든 (구종,존) 조합에 점수를 매긴 정렬 가능한 리스트"로 잡아, Top-1/2/위험을 같은 리스트에서 추출.

## D13. 서빙 [확정 — 타이밍은 실측 후]

- 추론 코어: 상태 → transition 필터 → Q 정렬 → Top-1/2/위험. (실시간/사후 공통)
- **사전계산 캐시**: 라이브에 NN을 매번 돌리지 않고, 가능한 상태 조합 결과를 경기 전 배치로 미리 계산해 룩업.
- 선수별 사전계산 테이블: 클러스터 ID + 시즌 구사율/성적 벡터를 서빙 환경에 미리 적재(라이브 중 외부 조회 불가).
- 타이밍("다음 투구 전에 예측이 뜨는가")은 본질적으로 실측 문제 → 9월 리허설로 검증. 그 전엔 타이밍 외 전부 완성.

---

## 금지사항 (에이전트 절대 위반 금지)

1. 타자 약점유형 클러스터링에 **절대 생산성 지표(wOBA, HR, 타율 등)를 넣지 말 것.** 강타자/약타자로 갈려 "약타자엔 아무거나" 함정. 약점 축은 상대적 강약만. (위협도 축에만 절대성적 허용)
2. **투수 클러스터로 타자 데이터를 쪼개지 말 것.** 두 축은 독립. 상호작용은 Q함수가 학습한다.
3. 라이브에서 못 받는 피처를 학습에 넣지 말 것 (학습=서빙 입력 일치, D3).
4. 존을 절대좌표로 처리하지 말 것. 타자 기준 상대좌표로 일관 (D5).
5. 검증 단계 데이터는 raw 그대로 캐시. 임의 필터링·가공 금지.
6. 단순 분류기로 회귀하지 말 것 (병살·유인구·승부회피 표현 불가, D6).
7. 설계 결정을 "개선"이라며 임의로 뒤집지 말 것. 변경 필요 시 사람에게 먼저 확인.

---

## 미해결 목록 (다음에 확정할 것)

- reward 역전파: 타석 종료 RE24를 투구들에 어떻게 귀속? (D9)
- 추천 결과 데이터 구조(레이어 3↔4 인터페이스) 확정. (작업순서 6번, 당겨서 권장)
- 구종 3군 매핑 / 거친 존 그리드 — 실제 데이터 분포 확인 후 확정. (D5, D11)
- 점수차 구간 기준. (D4)
- 투수 클러스터 피처를 타자와 같은 깊이로 확정. (D11)
- "승부 회피"를 외곽 존 가치로 표현 가능한지 데이터 확인. (D5)
- 핸드니스를 클러스터 피처에 포함할지 / 상태에만 둘지. (D5, D11)
- 필터 임계값 튜닝 / off-policy 평가 방법. (D10)
