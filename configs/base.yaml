project:
  name: baseball-zerobase
  version: 0.1.0

scope:
  league: MLB
  player_role: starting_pitcher
  excludes:
    - bullpen_recommendation
    - neural_model
    - recommendation_engine
    - api
    - web_ui

paths:
  data: data
  artifacts: artifacts
  reports: reports/generated

starter:
  prior_two_season_pitches: 1000

baseline:
  transition_min_support: 50
  behavior_min_support: 25

simulation:
  trials: 2000
  max_pitches_per_inning: 150
