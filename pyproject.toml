[project]
name = "baseball-zerobase"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.14"
dependencies = [
    "pandas>=3.0.3",
    "pyarrow>=24.0.0",
    "pybaseball>=2.2.7",
]

[dependency-groups]
dev = [
    "ruff>=0.15.15",
]
